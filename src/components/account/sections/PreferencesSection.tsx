'use client'

import AccountAccordionSection from '../AccountAccordionSection'

export const SECTION_ID = 'preferences'

export default function PreferencesSection() {
  return (
    <AccountAccordionSection value={SECTION_ID} title="Mes préférences">
      {/* Contenu à venir */}
      <div />
    </AccountAccordionSection>
  )
}
